# Deployed separately from the Pages project in the repo root wrangler.toml.
# Run wrangler from inside this directory so it picks up this config.
name = "dinescores-portal-relay"
main = "src/index.js"
compatibility_date = "2026-01-01"

# RELAY_TOKEN is a secret, never a [vars] entry:
#   npx wrangler secret put RELAY_TOKEN
